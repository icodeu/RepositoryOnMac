说明：

运行：  python main.py

url.config 
   存储文件中配置要抓取图片的文章地址，一行一个链接，其中链接地址如果存在锚点请删除，支持的链接地址形式如url.config文件中所示

images 文件夹中存放抓下来的图片（最好以缩略图快速查看是否抓取成功了）

注：
此版本存在问题：图片相对路径转绝对路径时，如果相对路径为 ../ 等这种格式时，则不能转换为正确的绝对路径